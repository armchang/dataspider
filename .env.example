POSTGRES_HOST=localhost
POSTGRES_PORT=5432
POSTGRES_DB=dataspider
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres
POSTGRES_MAINTENANCE_DB=postgres

# Alternatively, provide one connection string instead of the settings above:
# DATABASE_URL=postgresql://postgres:postgres@localhost:5432/dataspider
